import { MessageDirectiveDirective } from './message-directive.directive';

describe('MessageDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
